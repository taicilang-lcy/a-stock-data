# a-stock-data 运行依赖
# 安装：pip install -r requirements.txt
#
# 版本说明：
# - mootdx 下界 >=0.10.5；不锁上界——0.11.x 的 BESTIP 空串 bug（#26）已由 SKILL.md 的
#   tdx_client() helper 从代码层规避（对 0.10/0.11 通用）。锁 <0.11 反而会在干净
#   Python 3.9 下因 numpy/pandas 二进制不兼容导致 import mootdx 崩溃。
# - 其余为宽松下界，保证已验证可用的最低版本；无特殊原因不建议锁死上界。
mootdx>=0.10.5
requests>=2.25
pandas>=1.3
lxml>=4.6              # pd.read_html 的 HTML 解析后端（pandas 核心包不含，干净安装必需）
urllib3>=1.26          # em_get 自动重试用到 Retry(allowed_methods=...)，1.26 才有该字段
stockstats>=0.4
